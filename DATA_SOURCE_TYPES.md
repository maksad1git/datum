# Система типов источников данных в DATUM

## Обзор

В системе DATUM реализована трехуровневая система классификации данных по источникам:

- **MON (Мониторинг)** - данные, полученные через мониторинговые замеры
- **EXP (Эксперт)** - данные, полученные от экспертов
- **AI (Искусственный интеллект)** - данные, полученные через анализ ИИ
- **CUSTOM (Смешанные)** - комбинация разных типов данных (только для формул и метрик)

## Где используются типы данных

### 1. Коэффициенты (Coefficient)

**Поле:** `data_type` (MON/EXP/AI)

**Назначение:** Определяет базовый тип коэффициента - какого рода данные он обычно представляет.

**Пример:**
- Коэффициент "Вес товара" - MON (измеряется при мониторинге)
- Коэффициент "Качество упаковки" - EXP (оценка эксперта)
- Коэффициент "Распознанный текст на этикетке" - AI (получен через ИИ)

### 2. Наблюдения (Observation)

**Поле:** `data_source_type` (MON/EXP/AI)

**Назначение:** Указывает, КАК ИМЕННО было получено конкретное значение наблюдения.

**Важно:** Для любого коэффициента можно создать наблюдение с любым типом источника данных!

**Пример:**
- Коэффициент "Вес товара" (data_type=MON) может иметь наблюдения:
  - Observation 1: value=150г, data_source_type=MON (взвешен на весах)
  - Observation 2: value=145г, data_source_type=EXP (оценка эксперта "на глаз")

### 3. Визиты (Visit)

**Поле:** `data_source_type` (MON/EXP/AI)

**Назначение:** Определяет тип визита в целом - это мониторинговый визит или экспертный.

**Связь с наблюдениями:** Во время заполнения визита, для каждого коэффициента можно выбрать, как было получено значение (мониторинг или эксперт).

### 4. Формулы (Formula)

**Поле:** `source_data_type` (MON/EXP/AI/CUSTOM)

**Назначение:** Определяет, какой тип данных использовать при расчете по формуле.

**Опции:**
- **MON** - использовать только наблюдения с data_source_type='MON'
- **EXP** - использовать только наблюдения с data_source_type='EXP'
- **AI** - использовать только наблюдения с data_source_type='AI'
- **CUSTOM** - использовать смешанные данные (комбинация типов)

**Пример:**
- Формула "Средний вес по мониторингу" (source_data_type=MON)
- Формула "Средний вес по оценке экспертов" (source_data_type=EXP)
- Формула "Комбинированная оценка веса" (source_data_type=CUSTOM)

### 5. Метрики (Metric)

**Поле:** `source_data_type` (MON/EXP/AI/CUSTOM)

**Назначение:** Определяет, какой тип данных использовать для расчета метрики.

**Связь с формулой:** Метрика может иметь свой собственный `source_data_type`, который может отличаться от типа связанной формулы. Это дает гибкость в настройке расчетов.

**Примеры:**
- Метрика "Дистрибуция по мониторингу" (source_data_type=MON)
- Метрика "Дистрибуция по экспертным оценкам" (source_data_type=EXP)
- Метрика "Дистрибуция по анализу ИИ" (source_data_type=AI)
- Метрика "Общая дистрибуция" (source_data_type=CUSTOM)

## Что НЕ имеет типов данных

### Атрибуты товаров (AttributeDefinition, ProductAttributeValue)

**Почему:** Атрибуты товаров - это статические характеристики товара (вес, размер, проба металла, тип камня и т.д.). Они не зависят от типа визита или способа сбора данных.

**Пример:**
- Вес изделия: 5.5г - это характеристика товара, не зависящая от того, как её измерили
- Проба металла: 585 - это свойство товара, не наблюдение

## Правила разделения данных

1. **Мониторинговые и экспертные данные НЕ должны смешиваться** при расчетах (если не указано source_data_type=CUSTOM)

2. **Каждое наблюдение** имеет свой тип источника данных, независимо от типа коэффициента

3. **Формулы и метрики** могут работать как с одним типом данных, так и со смешанными (CUSTOM)

4. **При визитах** пользователь может выбирать для каждого коэффициента, как было получено значение

## Примеры использования

### Сценарий 1: Мониторинговый визит

- Создается визит с data_source_type='MON'
- Для каждого коэффициента создаются наблюдения
- Пользователь может выбрать для каждого наблюдения:
  - MON - если значение получено через замер
  - EXP - если значение оценено экспертом

### Сценарий 2: Расчет метрики по мониторинговым данным

- Метрика "Средняя цена (мониторинг)" имеет source_data_type='MON'
- Связана с формулой расчета средней цены
- При расчете используются ТОЛЬКО наблюдения с data_source_type='MON'
- Экспертные оценки игнорируются

### Сценарий 3: Расчет метрики по смешанным данным

- Метрика "Общая дистрибуция" имеет source_data_type='CUSTOM'
- При расчете используются все наблюдения, независимо от типа источника

## База данных

### Модели с полем data_type/source_data_type:

```python
# Коэффициенты
Coefficient.data_type           # MON, EXP, AI

# Формулы
Formula.source_data_type        # MON, EXP, AI, CUSTOM

# Метрики
Metric.source_data_type         # MON, EXP, AI, CUSTOM

# Визиты
Visit.data_source_type          # MON, EXP, AI

# Наблюдения
Observation.data_source_type    # MON, EXP, AI
```

### Константы (core/constants.py):

```python
DATA_SOURCE_CHOICES = [
    ('MON', 'Мониторинг'),
    ('EXP', 'Эксперт'),
    ('AI', 'ИИ'),
]

FORMULA_SOURCE_CHOICES = [
    ('MON', 'Мониторинг'),
    ('EXP', 'Эксперт'),
    ('AI', 'ИИ'),
    ('CUSTOM', 'Смешанные'),
]
```

## Визуальное отображение

В интерфейсе типы данных отображаются цветными бейджами:

- 🔵 **Мониторинг** - синий бейдж с иконкой линейки
- 🟢 **Эксперт** - зеленый бейдж с иконкой эксперта
- 🟣 **ИИ** - фиолетовый бейдж с иконкой процессора
- 🟡 **Custom** - желтый бейдж с иконкой шестеренки
